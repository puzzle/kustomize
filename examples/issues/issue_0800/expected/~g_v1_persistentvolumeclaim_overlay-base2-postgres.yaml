apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    component: postgres
  name: overlay-base2-postgres
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: do-block-storage
