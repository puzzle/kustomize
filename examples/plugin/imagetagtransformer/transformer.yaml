apiVersion: builtin
kind: ImageTagTransformer
metadata:
  name: imagetagtransformer
imageTag:
  name: nginx
  newTag: v2
# The following fields can be transformed
# aside from the harcoded image field
# in containers and initContainers
fieldSpecs:
- path: spec/simpletext
